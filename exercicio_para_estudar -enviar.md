# Atividade de Treinamento: Programação para Internet II (Aulas 01-04)

Esta atividade foi elaborada para reforçar os conceitos de HTML Semântico, Git, Variáveis e Operadores em JavaScript. Abaixo de cada pergunta, você encontrará o passo a passo para chegar à resposta correta.

---

### Questão 1: Estrutura Semântica
Identifique qual tag HTML deve ser usada para envolver o bloco de navegação principal do seu site (o menu) e explique por que não devemos usar apenas uma `<div>`.


---

### Questão 2: Fluxo de Trabalho Git
Você acabou de criar um novo arquivo chamado `contato.html`. Escreva a sequência exata de comandos Git para registrar esse arquivo no seu repositório local e enviá-lo para o GitHub.

---

### Questão 3: Variáveis e Reatribuição
Observe o código abaixo e determine o que será impresso no console:
```javascript
let usuario = "Ana";
const ID = 101;
usuario = "Carlos";
// ID = 102; // Esta linha causaria erro?
console.log(usuario);
```

---

### Questão 4: Tipos de Dados (typeof)
Sem executar o código, identifique o tipo de dado (resultado do `typeof`) para cada variável:
```javascript
let a = "10";
let b = 10;
let c = (10 > 5);
```


---

### Questão 5: Operadores e Precedência
Calcule o valor da variável `resultado` seguindo as regras do JavaScript:
```javascript
let x = 10;
let y = 2;
let resultado = x + y * 5;
resultado++;
```

---

### Questão 6: Operadores Lógicos e Comparação Estrita
Qual será o valor booleano de `validacao`?
```javascript
let n1 = 5;
let n2 = "5";
let validacao = (n1 === n2) || (10 != 11);
```


